/** Matches CSS variables from next/font in src/app/layout.jsx */
export const FF = {
  sans: "var(--font-sans), system-ui, -apple-system, sans-serif",
  mono: "var(--font-mono), ui-monospace, monospace",
  display: "var(--font-display), Georgia, serif",
};
